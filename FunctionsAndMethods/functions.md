//function are of two types:-
# Build-in functions-:
these functions are written by user . Eg:- factorial,binomial,sum,product.

# user defined functions-:
these functions are inbuild function . Eg:- pow(power function), sqrt (square root function),max (maximum function),
sc.nextInt().
